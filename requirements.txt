# Runtime dependencies for Scenic Route Planner.
# Install with: pip install -r requirements.txt
# For reproducible CI installs, prefer requirements.lock.txt when present.

fastapi>=0.110
uvicorn[standard]>=0.29
requests>=2.31
httpx>=0.27
Pillow>=10.0
numpy>=1.26
networkx>=3.2
pytest>=8.0
