# --- Python ---
__pycache__/
*.py[cod]
*.egg-info/
.eggs/
build/
dist/
.venv/
.venv-lock/
venv/
env/
.mypy_cache/
.pytest_cache/
.ruff_cache/

# --- Generated data & caches (rebuilt at runtime; never commit) ---
# Satellite tiles, land-cover tiles, the SQLite store and any scored grids all
# live under data/ and can be regenerated, so they stay out of version control.
/data/
!/data/.gitkeep

# Shipped offline climate grid: app/data/climate_grid.npz (tracked; ~65 KB).

# --- Editor / OS noise ---
.vscode/
.idea/
.DS_Store
Thumbs.db

# --- Local backups / scratch ---
/backups/
*.v2bak
*.bak
*.tmp
scripts/_*.py
